name: leek
description: Leek - Reproductor de música
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  just_audio: ^0.9.40
  on_audio_query: ^2.9.0
  permission_handler: ^11.3.1
  audio_video_progress_bar: ^2.0.3
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.3
  video_player: ^2.9.1

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
